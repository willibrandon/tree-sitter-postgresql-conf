# PostgreSQL configuration file
# <- @comment

port = 5432
# <- @property
#    ^ @operator
#      ^ @number

shared_buffers = 128MB
#                ^ @number
#                   ^ @type

fsync = on
#       ^ @boolean

log_destination = stderr
#                 ^ @constant

listen_addresses = 'localhost'
#                  ^ @string

include_dir 'conf.d'
# <- @keyword.import
#           ^ @string.special.path
